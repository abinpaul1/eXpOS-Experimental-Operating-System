int main()
{
decl
    int temp, i, usertabledes;
    str uname;
enddecl

begin
    i = 0;
    while (i<8) do 
        temp = exposcall("Getuname",i);
        if (temp != -1) then
            write(temp);
        endif;
        i = i + 1;
    endwhile;
    return 0;
end

}
